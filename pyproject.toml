[project]
name = "python-htmlelements"
version = "0.5.0"
description = "Create HTML using python code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
license = { text = "MIT" }

[project.optional-dependencies]
django = ["django>=5.2.0"]
